{
  "name": "cloudqw-pass",
  "version": "0.0.1",
  "description": "A Generic CloudQ worker that receives a request and passes it to another api.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tom Wilson",
  "license": "MIT",
  "dependencies": {
    "cloudq-worker": "0.0.1",
    "request": "~2.10.0",
    "nconf": "~0.6.4"
  }
}
